export interface DIGIMON{
    id: number;
    name: string;
    img:string;
    level:string;
}

export interface ClientError {
    code: string;
    description: string;
}

export interface Nivel{
    name:string;
}